clear all;
close all;
clc;


x=[1 2 3 4];
h=[1 2 3 4 5 6];
conv(x,h)
h=fliplr(h);
X=length(x);
H=length(h);

for n=1:(H+X-1)
    for k=1:X
    if n-k>=0 && n-k<H
    y(n)=y(n)+x(k)*h(n-k+1);    
        end
    end
    
end
