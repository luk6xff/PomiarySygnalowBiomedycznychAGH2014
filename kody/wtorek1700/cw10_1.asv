clc;
clear all;
close all;

h=fir1(10,0.5);
x=rand(1,500)
d=conv(x,h);
d=d+0.1*randn(size(d))
hlms=1
hrls=1
mi=0.01
delt=0.01
hrls=zeros(1,1)

[hrls,h_n,e_n]=rls_lab(x,d,hrls,delt);
figure(1)
plot(abs(e_n),'.-r')
figure(2)
plot(abs(h_n),'o-g')
figure(3)
