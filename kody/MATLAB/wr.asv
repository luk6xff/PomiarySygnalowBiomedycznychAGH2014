clear all; close all; clc

N=1000;
aa=2;
bb=3;

b=zeros(2,1);
P=eye(2,2);

for n=1:N
    w=rand*20-10;
    x=[1 w];
    y=bb+aa*w;
    c=1/(x*P*x(n,:)'+1);
    K=P*x'*c;
    b=b+K*(y-x*b);
    P=P-K*x*P;
end